/**
 * @fileoverview this is a rule for not allow require some files
 * @author haoxutong
 * @copyright 2015 haoxutong. All rights reserved.
 * See LICENSE file in root directory for full license.
 */
"use strict";

//------------------------------------------------------------------------------
// Requirements
//------------------------------------------------------------------------------

var rule = require("../../../lib/rules/myconfig"),

    RuleTester = require("eslint").RuleTester;


//------------------------------------------------------------------------------
// Tests
//------------------------------------------------------------------------------

var ruleTester = new RuleTester();
ruleTester.run("myconfig", rule, {

    valid: [

        // give me some code that won't trigger a warning
    ],

    invalid: [
        {
            code: "let config=require('./config_file.js')",
            errors: [{
                message: "Fill me in.",
                type: "Me too"
            }]
        }
    ]
});
