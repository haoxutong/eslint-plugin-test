/**
 * @fileoverview this is a rule for not allow require some files
 * @author haoxutong
 * @copyright 2015 haoxutong. All rights reserved.
 * See LICENSE file in root directory for full license.
 */
"use strict";

//------------------------------------------------------------------------------
// Rule Definition
//------------------------------------------------------------------------------

module.exports = function(context) {

    // variables should be defined here

    //--------------------------------------------------------------------------
    // Helpers
    //--------------------------------------------------------------------------

    // any helper functions should go here or else delete this section

    //--------------------------------------------------------------------------
    // Public
    //--------------------------------------------------------------------------

    return {

        // give me methods
        CallExpression:function CallExpression(node) {
            var first = context.getTokens(node);
            if(first[2].value == '\'./config_file.js\''){
                context.report({node:node, message:"This is a package test rule, if you see this warning, this is successful!!"})
            }

        }
    }

};

module.exports.schema = [
    // fill in your schema
];
